function abrir(url)
 {
    if (url.length>0)
       if (confirm("Deseja realmente excluir?"))
      window.location = url;
 }

//------------------------------------------------------------------------//